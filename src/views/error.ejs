<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Error' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    
    
    <style>
        
        :root {
            --fritz-red: #DC2626;
            --fritz-black: #1A1A1A;
        }
        body {
            background: linear-gradient(135deg, #f5f5f5 0%, #e8e8e8 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .error-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            border-left: 5px solid var(--fritz-red);
        }
        .btn-fritz {
            background-color: var(--fritz-red);
            border: none;
            color: white;
            padding: 10px 20px;
            border-radius: 6px;
            text-decoration: none;
            display: inline-block;
        }
        .btn-fritz:hover {
            background-color: #c53030;
            color: white;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card error-card">
                    <div class="card-header bg-white border-0 text-center pt-4">
                        <i class="bi bi-exclamation-triangle-fill text-danger" style="font-size: 4rem;"></i>
                        <h2 class="mt-3 text-dark"><%= title || 'Error' %></h2>
                    </div>
                    <div class="card-body text-center py-4">
                        <h4 class="text-muted mb-3">
                            <%= message || 'Ha ocurrido un error inesperado' %>
                        </h4>
                        
                        <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
                            <div class="alert alert-warning text-start mt-3">
                                <small>
                                    <strong>Detalles técnicos:</strong><br>
                                    <code><%= error.stack %></code>
                                </small>
                            </div>
                        <% } %>
                        
                        <div class="mt-4">
                            <a href="/dashboard" class="btn btn-fritz me-2">
                                <i class="bi bi-house me-1"></i>Ir al Dashboard
                            </a>
                            <a href="/login" class="btn btn-outline-secondary">
                                <i class="bi bi-box-arrow-in-right me-1"></i>Volver al Login
                            </a>
                        </div>
                    </div>
                    <div class="card-footer bg-white border-0 text-center">
                        <small class="text-muted">
                            Sistema de Gestión Fritz C.A &copy; 2024
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>