<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <title>Reporte de Usuarios - FRITZ C.A</title>
    <style>
        /* ESTILOS SIMPLES Y COMPATIBLES */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            line-height: 1.3;
            color: #000000;
            padding: 15px;
            background: #ffffff;
        }
        
        .header {
            text-align: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #DC2626;
        }
        
        .header h1 {
            color: #DC2626;
            font-size: 20px;
            margin-bottom: 5px;
        }
        
        .header .subtitle {
            color: #666666;
            font-size: 14px;
        }
        
        .info-section {
            background: #f5f5f5;
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 15px;
        }
        
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 3px;
        }
        
        .info-label {
            font-weight: bold;
            color: #333333;
        }
        
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            font-size: 9px;
        }
        
        .table th {
            background: #DC2626;
            color: #ffffff;
            padding: 6px;
            text-align: left;
            border: 1px solid #cccccc;
            font-weight: bold;
        }
        
        .table td {
            padding: 5px;
            border: 1px solid #cccccc;
            vertical-align: top;
        }
        
        .table tr:nth-child(even) {
            background: #f9f9f9;
        }
        
        .badge {
            display: inline-block;
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 8px;
            font-weight: bold;
            text-align: center;
        }
        
        .badge-success {
            background: #d4edda;
            color: #155724;
        }
        
        .badge-warning {
            background: #fff3cd;
            color: #856404;
        }
        
        .badge-secondary {
            background: #e9ecef;
            color: #495057;
        }
        
        .text-center {
            text-align: center;
        }
        
        .footer {
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid #cccccc;
            text-align: center;
            font-size: 9px;
            color: #666666;
        }
        
        .no-data {
            text-align: center;
            padding: 30px;
            color: #666666;
            font-style: italic;
        }
    </style>
</head>
<body>
    <!-- Encabezado -->
    <div class="header">
        <h1>FRITZ C.A</h1>
        <div class="subtitle">Reporte de Usuarios</div>
    </div>
    
    <!-- Información general -->
    <div class="info-section">
        <div class="info-row">
            <span class="info-label">Fecha de generación:</span>
            <span><%= fechaGeneracion %></span>
        </div>
        <div class="info-row">
            <span class="info-label">Total de usuarios:</span>
            <span><%= totalUsuarios %></span>
        </div>
        <div class="info-row">
            <span class="info-label">Usuarios con equipos activos:</span>
            <span><%= totalConEquipos %></span>
        </div>
        <div class="info-row">
            <span class="info-label">Usuarios sin equipos activos:</span>
            <span><%= totalSinEquipos %></span>
        </div>
    </div>
    
    <!-- Tabla de usuarios -->
    <% if (usuarios && usuarios.length > 0) { %>
        <table class="table">
            <thead>
                <tr>
                    <th>Usuario</th>
                    <th>Cargo</th>
                    <th>Correo</th>
                    <th>RDP</th>
                    <th>Sede</th>
                    <th>Depto</th>
                    <th width="40" class="text-center">Total</th>
                    <th width="40" class="text-center">Activos</th>
                    <th width="60" class="text-center">Estado</th>
                </tr>
            </thead>
            <tbody>
                <% usuarios.forEach(function(usuario) { %>
                    <tr>
                        <td><%= usuario.nombre %> <%= usuario.apellido %></td>
                        <td><%= usuario.cargo %></td>
                        <td><%= usuario.correo %></td>
                        <td><%= usuario.rdpfis %> <%= usuario.rdpfin %></td>
                        <td><%= usuario.sede && usuario.sede.nombre ? usuario.sede.nombre : 'N/A' %></td>
                        <td><%= usuario.departamento && usuario.departamento.nombre ? usuario.departamento.nombre : 'N/A' %></td>
                        <td class="text-center"><%= usuario.equipos_totales_count || 0 %></td>
                        <td class="text-center"><%= usuario.equipos_activos_count || 0 %></td>
                        <td class="text-center">
                            <% 
                                const activos = usuario.equipos_activos_count || 0;
                                const total = usuario.equipos_totales_count || 0;
                                
                                if (activos > 0) {
                            %>
                                <span class="badge badge-success">Con equipos</span>
                            <% } else if (total > 0) { %>
                                <span class="badge badge-warning">Solo devueltos</span>
                            <% } else { %>
                                <span class="badge badge-secondary">Sin equipos</span>
                            <% } %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <div class="no-data">
            <h3>No hay usuarios registrados</h3>
            <p>No se encontraron usuarios en el sistema.</p>
        </div>
    <% } %>
    
    <!-- Pie de página -->
    <div class="footer">
        <div>Sistema de Gestión de Equipos - FRITZ C.A</div>
        <div>Generado el: <%= fechaGeneracion %></div>
    </div>
</body>
</html>