<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <style>
        @page {
            size: Letter landscape;
            margin: 15mm 10mm;
        }
        
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 12px;
            line-height: 1.4;
            margin: 0;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #DC2626;
            padding-bottom: 10px;
        }
        
        .logo-section {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 8px;
        }
        
        .logo {
            width: 50px;
            height: 40px;
            margin-right: 10px;
        }
        
        .header h1 {
            color: #DC2626;
            margin: 0;
            font-size: 24px;
        }

        .header h2 {
            color: #666;
            margin: 3px 0 0 0;
            font-size: 14px;
            font-weight: normal;
        }
        
        .metadata {
            display: flex;
            justify-content: space-between;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 8px 12px;
            border-radius: 6px;
            margin-bottom: 12px;
            border-left: 4px solid #DC2626;
        }
        
        .metadata-item {
            display: flex;
            flex-direction: column;
        }
        
        .metadata-label {
           font-weight: bold;
            color: #333;
        }
        
        .metadata-value {
            font-size: 11px;
            color: #1a1a1a;
            font-weight: bold;
        }
        
        .estadisticas-grid {
         display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .estadistica-card {
            background: #e9ecef;
            padding: 10px;
            border-radius: 5px;
            text-align: center;
        }
        
        .estadistica-numero {
             font-size: 18px;
            font-weight: bold;
            color: #DC2626;
        }
        
        .estadistica-texto {
          font-size: 9px;
            opacity: 0.9;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 8px;
            font-size: 8px;
            page-break-inside: auto;
        }
        
        thead {
            display: table-header-group;
        }
        
        tr {
            page-break-inside: avoid;
            page-break-after: auto;
        }
        
        th {
            background: linear-gradient(135deg, #DC2626 0%, #B91C1C 100%);
            color: white;
            padding: 6px 4px;
            text-align: left;
            border: 1px solid #B91C1C;
            font-weight: bold;
            font-size: 8px;
        }
        
        td {
            padding: 5px 4px;
            border: 1px solid #dee2e6;
            vertical-align: top;
            font-size: 8px;
        }
        
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        
        tr:hover {
            background-color: #e3f2fd;
        }
        
        .status-badge {
            padding: 3px 6px;
            border-radius: 10px;
            font-size: 7px;
            font-weight: bold;
            text-transform: uppercase;
            display: inline-block;
            text-align: center;
            min-width: 50px;
        }
        
        .status-activo { background: #d1fae5; color: #065f46; border: 1px solid #a7f3d0; }
        .status-inactivo { background: #f3f4f6; color: #374151; border: 1px solid #d1d5db; }
        .status-mantenimiento { background: #fef3c7; color: #92400e; border: 1px solid #fcd34d; }
        .status-desuso { background: #fce7f3; color: #be185d; border: 1px solid #f9a8d4; }
        
        .footer {
            margin-top: 15px;
            text-align: center;
            font-size: 8px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 8px;
        }
        
        .page-number:before {
            content: "Página " counter(page);
        }
        
        .no-data {
            text-align: center;
            padding: 30px;
            color: #666;
            font-style: italic;
            background: #f8f9fa;
            border-radius: 6px;
            margin: 20px 0;
        }
        
        .equipo-info {
            font-size: 10px;
            line-height: 1.2;
        }
        
       
        
        .table-container {
            overflow: hidden;
        }
        
        .col-equipo { width: 100px; }
        .col-descripcion { width: 90px; }
        .col-ip { width: 70px; }
        .col-serial { width: 80px; }
        .col-sede { width: 80px; }
        .col-ubicacion { width: 70px; }
        .col-estado { width: 50px; }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo-section">
            <div class="logo">
                <!-- Logo de Fritz C.A -->
                <div style="width: 50px; height: 40px; background: #DC2626; color: white; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 10px; border-radius: 4px;">
                    FRITZ
                </div>
            </div>
            <div>
                <h1><%= titulo %></h1>
                <h2>Sistema de Gestión de Mikrotiks</h2>
            </div>
        </div>
    </div>
    
    <div class="metadata">
        <div class="metadata-item">
            <span class="metadata-label">FECHA DE GENERACIÓN</span>
            <span class="metadata-value"><%= fecha %></span>
        </div>
        <div class="metadata-item">
            <span class="metadata-label">TOTAL DE SEDES</span>
            <span class="metadata-value">
                <% 
                const sedesUnicas = [...new Set(mikrotiks.map(m => m.sede_id).filter(Boolean))];
                %>
                <%= sedesUnicas.length %> sedes
            </span>
        </div>
        <div class="metadata-item">
            <span class="metadata-label">HORA</span>
            <span class="metadata-value"><%= new Date().toLocaleTimeString('es-ES') %></span>
        </div>
    </div>
    
    <div class="estadisticas-grid">
        <div class="estadistica-card total">
            <span class="estadistica-numero"><%= total %></span>
            <span class="estadistica-texto">TOTAL</span>
        </div>
        <div class="estadistica-card activos">
            <span class="estadistica-numero"><%= estadisticas.activos %></span>
            <span class="estadistica-texto">ACTIVOS</span>
        </div>
        <div class="estadistica-card inactivos">
            <span class="estadistica-numero"><%= estadisticas.inactivos %></span>
            <span class="estadistica-texto">INACTIVOS</span>
        </div>
        <div class="estadistica-card mantenimiento">
            <span class="estadistica-numero"><%= estadisticas.mantenimiento %></span>
            <span class="estadistica-texto">MANTENIMIENTO</span>
        </div>
        <div class="estadistica-card desuso">
            <span class="estadistica-numero"><%= estadisticas.desuso %></span>
            <span class="estadistica-texto">DAÑADOS</span>
        </div>
    </div>
    
    <% if (mikrotiks && mikrotiks.length > 0) { %>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th class="col-equipo">EQUIPO/MODELO</th>
                    <th class="col-descripcion">DESCRIPCIÓN</th>
                    <th class="col-ip">DIRECCIÓN IP</th>
                    <th class="col-serial">NÚMERO DE SERIE</th>
                    <th class="col-sede">SEDE</th>
                    <th class="col-ubicacion">UBICACIÓN</th>
                    <th class="col-estado">ESTADO</th>
                </tr>
            </thead>
            <tbody>
                <% 
                let currentSede = null;
                mikrotiks.forEach(mikrotik => { 
                    const sedeChanged = currentSede !== mikrotik.sede_id;
                    currentSede = mikrotik.sede_id;
                %>
                    <% if (sedeChanged && mikrotik.sede) { %>
                    <tr>
                        <td colspan="7" style="background: #e3f2fd; padding: 4px 8px; font-weight: bold; border-left: 3px solid #DC2626; font-size: 9px;">
                            SEDE: <%= mikrotik.sede.nombre %>
                        </td>
                    </tr>
                    <% } %>
                    
                    <tr>
                        <td class="col-equipo">
                            <div class="equipo-info">
                                <% if (mikrotik.stock_equipos) { %>
                                    <strong><%= mikrotik.stock_equipos.marca %>
                                    <%= mikrotik.stock_equipos.modelo %></strong><br>
                                    <span style="font-size: 10px; color: #666;">
                                        <%= mikrotik.stock_equipos.tipo_equipo.nombre %>
                                    </span>
                                <% } else { %>
                                    <em>No asignado</em>
                                <% } %>
                            </div>
                        </td>
                        <td class="col-descripcion" style="font-size: 10px;">
                            <%= mikrotik.descripcion || 'Mikrotik' %>
                        </td>
                        <td class="col-ip" style="font-size: 10px;">
                            <% if (mikrotik.ip_mikrotik) { %>
                                <span class="ip-address"><%= mikrotik.ip_mikrotik %></span>
                            <% } else { %>
                                <em>-</em>
                            <% } %>
                        </td>
                        <td class="col-serial" style="font-size: 10px;">
                            <%= mikrotik.cereal_mikrotik || '-' %>
                        </td>
                        <td class="col-sede" style="font-size: 10px;">
                            <%= mikrotik.sede ? mikrotik.sede.nombre : 'Sin sede' %>
                        </td>
                        <td class="col-ubicacion" style="font-size: 10px;">
                            <%= mikrotik.ubicacion %>
                        </td>
                        <td class="col-estado" style="font-size: 10px;">
                            <span class="status-badge status-<%= mikrotik.estado %>">
                                <%= mikrotik.estado.charAt(0).toUpperCase() + mikrotik.estado.slice(1) %>
                            </span>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
    <% } else { %>
    <div class="no-data">
        <h3>No se encontraron mikrotiks</h3>
        <p>No hay mikrotiks registrados en el sistema para generar el reporte.</p>
    </div>
    <% } %>
    
    <div class="footer">
        <p><strong>FRITZ C.A - Sistema de Gestión de Mikrotiks</strong> | Reporte generado automáticamente</p>
        <p class="page-number"></p>
    </div>
</body>
</html>