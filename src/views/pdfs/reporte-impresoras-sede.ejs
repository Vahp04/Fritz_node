<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <style>
        /* Estilos específicos para reporte por sede */
        @page {
            size: A4 landscape;
            margin: 15mm 10mm;
        }
        
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 12px;
            line-height: 1.4;
            margin: 0;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
            border-bottom: 2px solid #DC2626;
            padding-bottom: 10px;
        }
        
        .logo-section {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 8px;
        }
        
        .logo {
            width: 50px;
            height: 40px;
            margin-right: 10px;
        }
        
         .header h1 {
            color: #DC2626;
            margin: 0;
            font-size: 24px;
        }

        .header h2 {
            color: #666;
            margin: 3px 0 0 0;
            font-size: 14px;
            font-weight: normal;
        }
        
        .metadata {
            display: flex;
            justify-content: space-between;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 8px 12px;
            border-radius: 6px;
            margin-bottom: 12px;
            border-left: 4px solid #DC2626;
        }
        
        .sede-info {
            background: linear-gradient(135deg, #eb8585 0%, #aa2a2a 100%);
            color: white;
            padding: 10px 15px;
            border-radius: 8px;
            margin-bottom: 12px;
            text-align: center;
        }
        
        .sede-nombre {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .sede-details {
            display: flex;
            justify-content: center;
            gap: 20px;
            font-size: 15px;
            opacity: 1.0;
        }
        
      
        
        .metadata-item {
            display: flex;
            flex-direction: column;
        }
        
        .metadata-label {
          font-weight: bold;
            color: #333;
        }
        
        .metadata-value {
            font-size: 11px;
            color: #1a1a1a;
            font-weight: bold;
        }
        
        .estadisticas-grid {
          display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .estadistica-card {
            background: #e9ecef;
            padding: 10px;
            border-radius: 5px;
            text-align: center;
        }
        
        .estadistica-numero {
              font-size: 18px;
            font-weight: bold;
            color: #DC2626;
        }
        
        .estadistica-texto {
            font-size: 9px;
            opacity: 0.9;
        }
        
        table {
              width: 100%;
            border-collapse: collapse;
            margin-top: 8px;
            font-size: 8px;
            page-break-inside: auto;
        }
        
        th {
            background: linear-gradient(135deg, #DC2626 0%, #B91C1C 100%);
            color: white;
            padding: 6px 4px;
            text-align: left;
            border: 1px solid #B91C1C;
            font-weight: bold;
        }
        
        td {
            padding: 5px 4px;
            border: 1px solid #dee2e6;
            vertical-align: top;
        }
        
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        
        .status-badge {
            padding: 3px 6px;
            border-radius: 10px;
            font-size: 7px;
            font-weight: bold;
            text-transform: uppercase;
            display: inline-block;
            text-align: center;
            min-width: 50px;
        }
        
        .status-activa { background: #d1fae5; color: #065f46; border: 1px solid #a7f3d0; }
        .status-inactiva { background: #f3f4f6; color: #374151; border: 1px solid #d1d5db; }
        .status-mantenimiento { background: #fef3c7; color: #92400e; border: 1px solid #fcd34d; }
        .status-obsoleta { background: #fce7f3; color: #be185d; border: 1px solid #f9a8d4; }
        
       
        
        .departamento-header {
            background: #e3f2fd;
            padding: 4px 8px;
            margin: 8px 0 4px 0;
            border-left: 3px solid #f32121;
            font-weight: bold;
            font-size: 9px;
            color: #d21f19;
        }
        
        .footer {
            margin-top: 15px;
            text-align: center;
            font-size: 8px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 8px;
        }
        
        .no-data {
            text-align: center;
            padding: 30px;
            color: #666;
            font-style: italic;
            background: #f8f9fa;
            border-radius: 6px;
            margin: 20px 0;
        }
        
        .equipo-info {
            font-size: 10px;
            line-height: 1.2;
        }
        
        .toner-info {
            font-size: 8px;
            font-weight: bold;
        }
        
        /* Column widths optimizadas */
        .col-id { width: 25px; }
        .col-nombre { width: 90px; }
        .col-equipo { width: 100px; }
        .col-ip { width: 70px; }
        .col-serial { width: 80px; }
        .col-departamento { width: 100px; }
        .col-ubicacion { width: 80px; }
        .col-estado { width: 50px; }
        .col-toner { width: 90px; }
        .col-contador { width: 50px; }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo-section">
            <div class="logo">
                <div style="width: 50px; height: 40px; background: #DC2626; color: white; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 10px; border-radius: 4px;">
                    FRITZ
                </div>
            </div>
            <div>
                <h1><%= titulo %></h1>
                <h2>Reporte Específico por Sede</h2>
            </div>
        </div>
    </div>
    
    <div class="sede-info">
        <div class="sede-nombre">
             <%= sede.nombre %>
        </div>
        <div class="sede-details" style="font-size: 10px;">
            <span>ID: <%= sede.id %></span>
            <span>•</span>
            <span>Total Impresoras: <%= total %></span>
        </div>
    </div>
    
    <div class="metadata">
        <div class="metadata-item">
            <span class="metadata-label">FECHA DE GENERACIÓN</span>
            <span class="metadata-value"><%= fecha %></span>
        </div>
        <div class="metadata-item">
            <span class="metadata-label">HORA</span>
            <span class="metadata-value"><%= new Date().toLocaleTimeString('es-ES') %></span>
        </div>
        <div class="metadata-item">
            <span class="metadata-label">DEPARTAMENTOS</span>
            <span class="metadata-value">
                <% 
                const departamentosUnicos = [...new Set(impresoras.map(i => i.departamento_id).filter(Boolean))];
                %>
                <%= departamentosUnicos.length %> departamentos
            </span>
        </div>
    </div>
    
    <div class="estadisticas-grid">
        <div class="estadistica-card total">
            <span class="estadistica-numero"><%= total %></span>
            <span class="estadistica-texto">TOTAL</span>
        </div>
        <div class="estadistica-card activas">
            <span class="estadistica-numero"><%= estadisticas.activas %></span>
            <span class="estadistica-texto">ACTIVAS</span>
        </div>
        <div class="estadistica-card inactivas">
            <span class="estadistica-numero"><%= estadisticas.inactivas %></span>
            <span class="estadistica-texto">INACTIVAS</span>
        </div>
        <div class="estadistica-card mantenimiento">
            <span class="estadistica-numero"><%= estadisticas.mantenimiento %></span>
            <span class="estadistica-texto">MANTENIMIENTO</span>
        </div>
        <div class="estadistica-card obsoletas">
            <span class="estadistica-numero"><%= estadisticas.obsoletas %></span>
            <span class="estadistica-texto">OBSOLETAS</span>
        </div>
    </div>
    
    <% if (impresoras && impresoras.length > 0) { %>
    <table>
        <thead>
            <tr>
                
                <th class="col-nombre">IMPRESORA</th>
                <th class="col-equipo">EQUIPO/MODELO</th>
                <th class="col-ip">DIRECCIÓN IP</th>
                <th class="col-serial">NÚMERO DE SERIE</th>
                <th class="col-departamento">DEPARTAMENTO</th>
                <th class="col-ubicacion">UBICACIÓN</th>
                <th class="col-estado">ESTADO</th>
                <th class="col-toner">TONER</th>
             
            </tr>
        </thead>
        <tbody>
            <% 
            let currentDepartamento = null;
            impresoras.forEach(impresora => { 
                const departamentoChanged = currentDepartamento !== impresora.departamento_id;
                currentDepartamento = impresora.departamento_id;
            %>
                <% if (departamentoChanged && impresora.departamento) { %>
                <tr>
                    <td colspan="10" class="departamento-header" style="font-size: 8px;">
                         DEPARTAMENTO: <%= impresora.departamento.nombre %>
                    </td>
                </tr>
                <% } %>
                
                <tr>
              
                    <td class="col-nombre" style="font-size: 10px;">
                        <strong><%= impresora.nombre %></strong>
                    </td>
                    <td class="col-equipo">
                        <div class="equipo-info">
                            <% if (impresora.stock_equipos) { %>
                                <strong><%= impresora.stock_equipos.marca %>
                                <%= impresora.stock_equipos.modelo %><br>
                               <%= impresora.stock_equipos.tipo_equipo.nombre %></strong><br>
                            <% } else { %>
                                <em>No asignado</em>
                            <% } %>
                        </div>
                    </td>
                    <td class="col-ip" style="font-size: 10px;">
                        <% if (impresora.ip_impresora) { %>
                            <span class="ip-address"><%= impresora.ip_impresora %></span>
                        <% } else { %>
                            <em>-</em>
                        <% } %>
                    </td>
                    <td class="col-serial" >
                        <%= impresora.cereal_impresora || '-' %>
                    </td>
                    <td class="col-departamento" style="font-size: 10px;">
                        <%= impresora.departamento ? impresora.departamento.nombre : 'Sin depto.' %>
                    </td>
                    <td class="col-ubicacion" style="font-size: 8px;">
                        <%= impresora.ubicacion %>
                    </td>
                    <td class="col-estado" style="font-size: 10px;">
                        <span class="status-badge status-<%= impresora.estado_impresora %>">
                            <%= impresora.estado_impresora.charAt(0).toUpperCase() + impresora.estado_impresora.slice(1) %>
                        </span>
                    </td>
                    <td class="col-toner">
                        <% if (impresora.toner) { %>
                            <div class="toner-info">
                                <%= impresora.toner %>
                            </div>
                        <% } else { %>
                            <em>-</em>
                        <% } %>
                    </td>
                 
                </tr>
            <% }); %>
        </tbody>
    </table>
    <% } else { %>
    <div class="no-data">
        <h3>No se encontraron impresoras</h3>
        <p>No hay impresoras registradas en la sede <%= sede.nombre %> para generar el reporte.</p>
    </div>
    <% } %>
    
    <div class="footer">
        <p><strong>FRITZ C.A - Sistema de Gestión de Impresoras</strong> | Reporte específico por sede</p>
        <p>Generado el <%= fecha %> a las <%= new Date().toLocaleTimeString('es-ES') %></p>
    </div>
</body>
</html>