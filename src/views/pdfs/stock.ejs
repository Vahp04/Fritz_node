<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Reporte de Stock de Equipos - FRITZ C.A</title>
    <style>
        body {
            font-family: 'Helvetica';
            font-size: 10px;
            line-height: 1.3;
            margin: 0;
            padding: 15px;
        }
        .header {
            text-align: center;
            margin-bottom: 15px;
            border-bottom: 2px solid #DC2626;
            padding-bottom: 8px;
        }
        .header h1 {
            color: #DC2626;
            margin: 0;
            font-size: 20px;
        }
        .header .subtitle {
            color: #666;
            font-size: 12px;
        }
        .info-section {
            margin-bottom: 12px;
            background: #f8f9fa;
            padding: 8px;
            border-radius: 4px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 4px;
        }
        .info-label {
            font-weight: bold;
            color: #333;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 8px;
            margin-bottom: 12px;
        }
        .stat-card {
            background: #e9ecef;
            padding: 8px;
            border-radius: 4px;
            text-align: center;
        }
        .stat-number {
            font-size: 16px;
            font-weight: bold;
            color: #DC2626;
        }
        .stat-label {
            font-size: 9px;
            color: #666;
        }
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
            font-size: 8px;
        }
        .table th {
            background-color: #DC2626;
            color: white;
            padding: 6px;
            text-align: left;
            border: 1px solid #ddd;
        }
        .table td {
            padding: 5px;
            border: 1px solid #ddd;
        }
        .table tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        .badge {
            padding: 2px 6px;
            border-radius: 3px;
            font-size: 7px;
            font-weight: bold;
            display: inline-block;
        }
        .badge-success {
            background-color: #d4edda;
            color: #155724;
        }
        .badge-warning {
            background-color: #fff3cd;
            color: #856404;
        }
        .badge-danger {
            background-color: #f8d7da;
            color: #721c24;
        }
        .summary {
            margin-top: 15px;
            padding: 8px;
            background: #e9ecef;
            border-radius: 4px;
        }
        .footer {
            margin-top: 15px;
            text-align: center;
            font-size: 9px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 8px;
        }
        .text-center {
            text-align: center;
        }
        .text-right {
            text-align: right;
        }
        .stock-bajo {
            background-color: #fff3cd !important;
        }
        .valor-column {
            text-align: right;
        }
        .logo-container {
            text-align: center;
            margin-bottom: 10px;
        }
        .logo {
            width: 60px;
            height: 40px;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo-container">
            <!-- Logo será insertado por PDFKit -->
            <div style="background: #DC2626; color: white; padding: 5px; display: inline-block; border-radius: 4px;">
                FRITZ C.A
            </div>
        </div>
        <h1>Reporte de Stock de Equipos</h1>
        <div class="subtitle">Sistema de Gestión de Inventario</div>
    </div>

    <div class="info-section">
        <div class="info-row">
            <span class="info-label">Fecha de generación:</span>
            <span>{{fechaGeneracion}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Total de equipos en inventario:</span>
            <span>{{stockEquipos.length}} tipos diferentes</span>
        </div>
    </div>

    <!-- Estadísticas principales -->
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-number">{{totalEquipos.toLocaleString()}}</div>
            <div class="stat-label">Total de Equipos</div>
        </div>
        <div class="stat-card">
            <div class="stat-number">{{totalDisponible.toLocaleString()}}</div>
            <div class="stat-label">Disponibles</div>
        </div>
        <div class="stat-card">
            <div class="stat-number">{{totalAsignado.toLocaleString()}}</div>
            <div class="stat-label">Asignados</div>
        </div>
        <div class="stat-card">
            <div class="stat-number">{{stockBajoCount.toLocaleString()}}</div>
            <div class="stat-label">Stock Bajo</div>
        </div>
    </div>

    <!-- Distribución por tipo -->
    <div class="summary">
        <h4 style="margin: 0 0 8px 0;">Distribución por Tipo de Equipo</h4>
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 8px;">
            {{#each equiposPorTipo}}
            <div style="background: white; padding: 6px; border-radius: 3px; border-left: 3px solid #DC2626;">
                <strong>{{@key}}</strong>: {{this}} equipos
            </div>
            {{/each}}
        </div>
    </div>

    {{#if stockEquipos.length}}
    <table class="table">
        <thead>
            <tr>
                <th>Tipo</th>
                <th>Marca</th>
                <th>Modelo</th>
                <th>Total</th>
                <th>Disp.</th>
                <th>Asig.</th>
                <th>Mín.</th>
                <th>Estado</th>
                <th class="valor-column">Valor Unit.</th>
            </tr>
        </thead>
        <tbody>
            {{#each stockEquipos}}
            {{#with this}}
            {{#assign "esStockBajo" cantidad_disponible <= minimo_stock}}
            {{#assign "valorUnitario" valor_adquisicion || 0}}
            <tr class="{{#if esStockBajo}}stock-bajo{{/if}}">
                <td>{{tipo_equipo.nombre}}</td>
                <td>{{marca}}</td>
                <td>{{modelo}}</td>
                <td class="text-center">{{cantidad_total}}</td>
                <td class="text-center">{{cantidad_disponible}}</td>
                <td class="text-center">{{cantidad_asignada}}</td>
                <td class="text-center">{{minimo_stock}}</td>
                <td class="text-center">
                    {{#if (eq cantidad_disponible 0)}}
                        <span class="badge badge-danger">Agotado</span>
                    {{else if (lte cantidad_disponible minimo_stock)}}
                        <span class="badge badge-warning">Bajo</span>
                    {{else}}
                        <span class="badge badge-success">OK</span>
                    {{/if}}
                </td>
                <td class="valor-column">${{formatCurrency valorUnitario}}</td>
            </tr>
            {{/with}}
            {{/each}}
        </tbody>
    </table>

    <!-- Resumen financiero -->
    <div class="summary">
        <h4 style="margin: 0 0 8px 0;">Resumen Financiero del Inventario</h4>
        <div class="info-row">
            <span class="info-label">Valor total del inventario:</span>
            <span class="info-label" style="font-size: 12px; color: #DC2626;">
                ${{formatCurrency valorTotal}}
            </span>
        </div>
        <div class="info-row">
            <span class="info-label">Total de equipos en inventario:</span>
            <span>{{totalEquipos.toLocaleString()}} unidades</span>
        </div>
        <div class="info-row">
            <span class="info-label">Equipos con stock bajo:</span>
            <span>{{stockBajoCount}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Tasa de asignación:</span>
            <span>{{#if totalEquipos}}{{formatPercent (divide totalAsignado totalEquipos)}}{{else}}0%{{/if}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">Valor promedio por equipo:</span>
            <span>${{formatCurrency (divide valorTotal totalEquipos)}}</span>
        </div>
    </div>
    {{else}}
    <div style="text-align: center; padding: 30px; color: #666;">
        <h3>No hay equipos en stock</h3>
    </div>
    {{/if}}

    <div class="footer">
        <div>Sistema de Gestión - FRITZ C.A</div>
        <div>Generado el {{fechaGeneracion}}</div>
    </div>
</body>
</html>